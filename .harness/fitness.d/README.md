# Fitness Checks

Starter architecture fitness checks grouped by framework or platform.

When vendored into a project, keep the directories and checks that apply to that project and delete the rest. The runner executes executable `*.sh` files recursively.
